<div class="container">
    <header>
        <nav class="navbar navbar-sticky navbar-expand-lg pt-0 pb-0">
            <div class="navbar-collapse justify-content-end" id="navbarColor01">
                {% if app.user %}
                {# user is logged in #}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {% if app.user.isGuide() %}
                                <span class="badge badge-primary">Guide</span> {{app.user.email}}
                            {% elseif app.user.isVisitor() %}
                                <span class="badge badge-primary">Visiteur</span>  {{app.user.email}}
                            {% endif %}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown2">
                            <a class="dropdown-item" href="{{path('dashboard')}}"><i class="fa fa-tachometer"></i> Mon dashboard</a>
                            <a class="dropdown-item" href="{{path('profile')}}"><i class="fa fa-user-circle-o"></i> Mon profil</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{path('app_logout')}}"><i class="fa fa-sign-out"></i> Se déconnecter</a>
                        </div>
                    </li>
                </ul>
                {% else %}
                {# user is not logged in #}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_register')}}">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
                    </li>
                </ul>
                {% endif %}
            </div>
        </nav>

        {% include ("includes/navbar.html.twig") %}
    </header>
</div>