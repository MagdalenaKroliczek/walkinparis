{% extends 'base.html.twig' %}

{% block title %}Hello HomepageController!{% endblock %}

{% block body %}
<style>
  .carousel-item .carousel-caption {
    background: rgba(0, 0, 0, 0.5)
  }

  .container-walk .container-walk_image {
      text-align: center;
      padding-left: 0;
  }
  .container-walk .container-walk_image img {
    height: 200px;
    width:auto;
  }
</style>

<div class="container-carousel">

  <div id="myCarrousel" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">
      <li data-target="#myCarrousel" data-slide-to="0" class="active"></li>
      <li data-target="#myCarrousel" data-slide-to="1"></li>
      <li data-target="#myCarrousel" data-slide-to="2"></li>
    </ol>
    <div class="carousel-inner">
      <div class="carousel-item active">
        <img src="{{ asset('assets/images/4.jpg') }}" class="d-block w-100" alt="Le Menoir Senchal">
        <div class="carousel-caption d-none d-md-block">
          <h3>Le Manoir Senchal</h3>
          <p>C'est un château abandonné avec de nombreuses histoires et secrets</p>
        </div>
      </div>
      <div class="carousel-item">
        <img src="{{ asset('assets/images/2.jpg') }}" class="d-block w-100" alt="Goussainville Vieux-Pays">
        <div class="carousel-caption d-none d-md-block">
          <h3>Goussainville Vieux-Pays</h3>
          <p>Un village abandonné aux portes de Paris</p>
        </div>
      </div>
      <div class="carousel-item">
        <img src="{{ asset('assets/images/eiffel.jpg') }}" class="d-block w-100" alt="La Tour Eiffel">
        <div class="carousel-caption d-none d-md-block">
          <h3>La tour Eiffel</h3>
          <p>Visite après le coucher du soleil</p>
        </div>
      </div>
    </div>
    <a class="carousel-control-prev" href="#myCarrousel" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#myCarrousel" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>

</div> <!-- end carroussel -->


<br>

<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <form class="d-flex">
      <input class="form-control me-2" type="search" placeholder="Recherche par mots-clés" aria-label="Search">
      <button class="btn btn-outline-success" type="submit">Chercher</button>
    </form>
  </div>
</nav>
<br>

<div class="container container-walk">

  {% for walk in walks %}
  <div class="row border mb-3 g-0">
    <div class="container-walk_image col-12 col-lg-4">
      <img src="{{ asset('uploads/' ~ walk.image) }}" alt="walk.title">
    </div>
    <div class="container-walk_description col-12 col-lg-8 p-3">
      <h4>{{walk.title}}</h4>
      <p>{{walk.description|striptags|length>250 ? walk.description|striptags|slice(0, 250) ~ '...' :  walk.description|striptags }}</p>
      <a href="#" class="btn btn-primary">Voir plus de détails</a>
    </div>
  </div>
  {% endfor %}

</div>

{% endblock %}