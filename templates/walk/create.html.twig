{% extends 'base.html.twig' %}

{% block title %}Hello WalkController!{% endblock %}

{% block body %}
<div class="card border-0">
  <div class="card-body">
    <h1 class="card-title">Créer une balade</h1>
      <p>Complétez les informations suivantes :</p>

      {{ form_start(walkForm) }}
      <hr>

      {{ form_row(walkForm.title, {'attr': {'placeholder': 'Saisir un titre de promenade'},'label': 'Titre' }) }}
      {{ form_row(walkForm.date) }}
      {{ form_row(walkForm.description,  {'attr': {'placeholder': 'Saisir une description'}, 'label': 'Description' }) }}
      {{ form_row(walkForm.price, {'attr': {'placeholder': 'Saisir un prix (€)'}, 'label': 'Prix' }) }}
      {{ form_row(walkForm.image, {
            label: 'Ajouter une photo'
        }) }}

      {{ form_widget(walkForm.save, { 'label': 'Ajouter' }) }} <a href="{{path('dashboard')}}">Annuler</a>

      {{ form_end(walkForm) }}
  </div>
</div>

{% endblock %}